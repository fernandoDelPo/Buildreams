<%- include('partials/head')%>

<link rel="stylesheet" href="/css/login.css">
<title>LogIn</title>


<%- include('partials/header')%>

<main>

    <h1>Acceso de Clientes</h1>

    <section>

        <div class="login">

            <div class="bloque">
                <h4 class="titulosinicio">Clientes Registrados</h4>

                <p>Si tiene una cuenta, inicie sesion con su e-mail y contraseña</p>

                <form action="/users/login" method="POST">

                    <label for="email" class="form-label" name="email" id="email" class="<%=locals.errors && errors.email ? 'is-danger' : '' %> " value="<%= locals.old && old.email %>"></label>>Email</label>
                    <% if(locals.errors && errors.email) { %>
                        <p class="feedback is-danger"><%= errors.email %> </p>
                    <% } %> 
                    <input type="text" name="email" id="email" class="form-control">

                    <label for="password" class="form-label">Contraseña</label>
                    <input class="form-input" type="password" name="password" id="password">

                    <label for="remember" class="form-label">Recordar mis datos</label>
                    <input class="form-input" type="checkbox" name="remember" id="remember">
                    <button class="submit" class="button-form" id="button-submit">Enviar</button>

                </form>
                <div class="nuevacuenta">
                    <a href="#">¿Olvidaste tu contraseña?</a>
                </div>

            </div>


            <div class="bloque">

                <h4 class="titulosinicio">Nuevos Clientes</h4>

                <p>¿No tienes una cuenta?</p>
                <p>Registrate y continua con tu compra, podrás acceder a beneficios exclusivos</p>

                <button class="botoninicio"><a href="/users/register">Crear cuenta</a></button>

            </div>

        </div>
    </section>

</main>

<%- include('partials/footer')%>
<%-include('./partials/script-bootstrap')%>

</body>

</html>